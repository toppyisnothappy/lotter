# Implementation Plan: POS UX Enhancements

**Track ID:** pos-ux-enhancements_20260315
**Spec:** spec.md
**Created:** 2026-03-15
**Status:** [x] Completed

## Overview

We will implement high-frequency item detection and category-based filtering. This involves adding new server actions to query the transaction history and updating the POS UI with reactive components for one-tap product adding.

## Phase 1: Data & API layer

Implement the backend logic to gather intelligence on product frequency and available categories.

### Tasks

- [x] Task 1.1: Create `getTopSellingProductsAction` in `src/features/pos/api/search.ts` to fetch products ordered by sale frequency.
- [x] Task 1.2: Create `getCategoriesAction` in `src/features/pos/api/search.ts` to get a unique list of categories for the organization.
- [x] Task 1.3: Update `searchProductAction` to support optional category filtering.

### Verification

- [x] Verify server actions return correct data via scratch script tests.

## Phase 2: UI Foundation

Create the visual components for the quick-pick grid and the category scroll bar.

### Tasks

- [x] Task 2.1: Implement `CategoryFilter` component in `src/features/pos/ui/`.
- [x] Task 2.2: Implement `QuickPickGrid` component in `src/features/pos/ui/`.
- [x] Task 2.3: Update `POSTerminal.tsx` layout to accommodate the new sections (Quick Pick at the top, Categories above the search results).

### Verification

- [x] Components render with dummy data and adhere to the glassmorphic design system.

## Phase 3: Integration & Logic

Connect the UI to real data and the cart state.

### Tasks

- [x] Task 3.1: Initialize `POSTerminal` with top-selling items and available categories on load.
- [x] Task 3.2: Hook up category selection to trigger filtered searches.
- [x] Task 3.3: Ensure clicking any Quick Pick item correctly calls `addItem` in `useCartStore`.

### Verification

- [x] Selecting a category updates the displayed list.
- [x] Tapping a "Most Sold" item adds it to the cart instantly.

## Final Verification

- [x] All acceptance criteria met
- [x] Responsiveness check (Ensuring the grid looks good on different screens)
- [x] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
