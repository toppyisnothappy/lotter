# Implementation Plan: Auth & Route Protection Remediation

**Track ID:** auth-leak-fix_20260321
**Spec:** spec.md
**Created:** 2026-03-21
**Status:** [x] Complete

## Overview

Repair the Next.js middleware and Auth.js configuration to ensure all protected store paths correctly require a valid user session. This includes verifying the database schema for Auth.js compatibility and ensuring roles are correctly enforced.

## Phase 1: Diagnosis & Schema Audit

Diagnose why the current auth state is not being correctly detected by the middleware.

### Tasks

- [x] Task 1.1: Confirm `DATABASE_URL` and `AUTH_SECRET` are correctly loaded by the environment.
- [x] Task 1.2: Verify the `user`, `session`, and `account` tables in the MySQL database match the Auth.js expected schema.
- [x] Task 1.3: Create a diagnostic route (`/api/auth/status`) to log the current session state and auth headers.

### Verification

- [x] Successfully fetch session state via the diagnostic route and confirm it return `null` when not logged in.

## Phase 2: Middleware & Config Repair

Ensure that `authorized` callbacks and the middleware `matcher` are correctly configured.

### Tasks

- [x] Task 2.1: Update `src/auth.config.ts` with strict redirects for unauthenticated users.
- [x] Task 2.2: Expand the middleware `matcher` in `src/middleware.ts` to cover all sensitive store paths.
- [x] Task 2.3: Implement a global "Auth Boundary" if necessary to prevent child components from rendering without auth.

### Verification

- [x] Navigation to `/pos` without session correctly triggers redirect to `/login`.
- [x] Navigation to `/inventory` without session correctly triggers redirect to `/login`.

## Phase 3: RBAC Enforcement

Ensure that different roles have proper access levels.

### Tasks

- [x] Task 3.1: Implement role checks in the `authorized` callback to prevent `clerk` from accessing `/admin` or `/organizations`.
- [x] Task 3.2: Update `src/app/api/auth/[...nextauth]/route.ts` to include role information in the JWT and session.

### Verification

- [x] Log in as a `clerk` and verify redirection when attempting to access `/admin`.
- [x] Log in as an `owner` and verify access to both `/pos` and `/admin`.

## Final Verification

- [x] All acceptance criteria met
- [x] Tests passing
- [x] Documentation updated (if applicable)
- [x] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
