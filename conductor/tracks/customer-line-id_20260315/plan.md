# Implementation Plan: Customer LINE ID Integration

**Track ID:** customer-line-id_20260315
**Spec:** spec.md
**Created:** 2026-03-15
**Status:** [x] Completed

## Overview

We will update the database schema, data entities, and UI components to support a new `line_id` field for customers.

## Phase 1: Database & Schema

Add the field to the source of truth.

### Tasks

- [x] Task 1.1: Add `line_id` column to `customers` table in `src/shared/lib/db/schema.ts`.
- [x] Task 1.2: Update the `Customer` interface in `src/entities/customer/model/types.ts`.
- [x] Task 1.3: Update `customerFormSchema` in `src/entities/customer/model/schema.ts`.

### Verification

- [x] Run `drizzle-kit generate` and `drizzle-kit push` to update the local database.

## Phase 2: Data Access Layer

Update the API functions to handle the new field.

### Tasks

- [x] Task 2.1: Update `mapToEntity` and `createCustomer` in `src/entities/customer/api/index.ts`.
- [x] Task 2.2: Update `updateCustomer` in `src/entities/customer/api/index.ts`.
- [x] Task 2.3: Update `createCustomerAction` and `updateCustomerAction` in `src/features/customer-crm/api/actions.ts`.

### Verification

- [x] Verify that a temporary script can create a customer with a LINE ID.

## Phase 3: UI Implementation

Update the forms and display components.

### Tasks

- [x] Task 3.1: Add the "LINE Account" field to `src/features/customer-crm/ui/CustomerForm.tsx`.
- [x] Task 3.2: Update `src/features/customer-crm/ui/CustomerTable.tsx` to show the LINE ID (optionally as an icon or text).
- [x] Task 3.3: Add LINE ID display to `src/features/customer-crm/ui/CustomerProfile.tsx`.

### Verification

- [x] Manually register a customer with a LINE ID and verify it appears on their profile.

## Final Verification

- [x] All acceptance criteria met
- [x] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
